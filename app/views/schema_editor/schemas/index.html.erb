<% @schema.each do |key, value| %>
  <% next if key == "foreign_keys" %>

  <table>
    <thead>
      <tr><td><%= key %></td></tr>
    </thead>
    <tbody>
      <% value.each do |name, type| %>
        <tr>
          <%= tag.td "#{name} (type: #{type.capitalize})" %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
